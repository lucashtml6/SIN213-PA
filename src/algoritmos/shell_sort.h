#ifndef SHELL_SORT_H
#define SHELL_SORT_H

/* Shell Sort - versao tradicional, com a sequencia de incrementos de Knuth.
   Ordena o vetor no proprio lugar, sem memoria adicional. */

void shellSort(int *vetor, int tamanho);

#endif
